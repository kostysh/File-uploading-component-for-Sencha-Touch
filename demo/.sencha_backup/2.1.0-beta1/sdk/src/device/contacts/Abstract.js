/**
 * @private
 */
Ext.define('Ext.device.contacts.Abstract', {
    /**
     * Returns a {@link Ext.data.Store} of the contacts available.
     * @return {Ext.data.Store} the contacts store
     */
    getStore: function() {
        return null;
    }
});
